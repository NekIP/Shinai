const state = {
	language: 'en-EN'
};

const getters = {

};

const actions = {

};

const mutations = {
	setLanguage(state, val) {
		state.language = val;
	}
};

export default {
	state,
	getters,
	actions,
	mutations
}
